#!/bin/bash --login

#[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
#rvm use 1.8.7@1111
rvm use 1.8.7@$$ --create
#gem install bundler
#bundle install --gemfile=Gemfilex
#rake spec
RESULT=$?
#/usr/local/rvm/bin/rvm --force gemset delete 1.8.7@$$
exit $RESULT
